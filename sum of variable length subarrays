class Solution:
    def subarraySum(self, nums: List[int]) -> int:
        res=[]
        for i in range(len(nums)):
            res.append(sum(nums[max(0, i - nums[i]):i+1]))
        return sum(res)
