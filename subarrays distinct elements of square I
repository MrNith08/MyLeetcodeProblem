class Solution:
    def sumCounts(self, nums: List[int]) -> int:
        sum=0
        for i in range(len(nums)):
            for j in range(i+1,len(nums)+1):
                subset=nums[i:j]
                sum+=len(set(subset))**2
        return sum
