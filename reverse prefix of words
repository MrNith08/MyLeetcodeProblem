class Solution:
    def reversePrefix(self, word: str, ch: str) -> str:
        i=0
        j=0
        for k in range(len(word)):
            if word[k]==ch:
                j=k
                break
        return (word[j::-1]+word[j+1:])
