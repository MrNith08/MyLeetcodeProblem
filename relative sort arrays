class Solution:
    def relativeSortArray(self, arr1: List[int], arr2: List[int]) -> List[int]:
        res=[]
        dic=Counter(arr1)
        for i in arr2:
            res.extend([i]*dic[i])
        for i in sorted(arr1):
            if i not in arr2:
                res.append(i)
        return res
