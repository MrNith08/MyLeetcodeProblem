class Solution:
    def similarPairs(self, words: List[str]) -> int:
        dic=Counter([frozenset(w) for w in words])
        ans = 0
        for f in dic.values():
            ans += f * (f - 1) // 2
        return ans
            
