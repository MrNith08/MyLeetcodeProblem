class Solution:
    def isCovered(self, ranges: List[List[int]], left: int, right: int) -> bool:
        for i in range(left,right+1):
            for j in ranges:
                res=False
                print(i,j)
                if j[0]<=i<=j[1]:
                    res=True
                    break
            if not res:
                return res
        return True
