class Solution:
    def isPossibleToSplit(self, nums: List[int]) -> bool:
        dic=Counter(nums)
        for i in dic.values():
            if i>2:
                return False
        return True
        
